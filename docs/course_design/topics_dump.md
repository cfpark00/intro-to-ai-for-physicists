# Topic Dump

Raw brain-dump of terms/concepts the course should touch. Preserved verbatim from a working session — to be grouped, deduped, and assigned to lectures in a later pass. Do not treat this as an outline; it's the input material, not the output.

## Dump 1 — 2026-05-25

in-context learning, meta learning, state space models, transformers, variational autoencoders, gans, diffusion models, reinforcement learning, mcts, search, system1 vs system 2, perception, action, compositional generalization, concept learning, emergence, scaling laws, reward hacking, open-endedness, quality-diversity, bayesian models, hyperparameters, optimization, high dimensional space, normalization, residual stream, mlps, cnns, nerfs, shape vs texture, continual learning, behavioral cloning, exploration vs exploitation, knightian uncertainty, aleatory/epistemic uncertainty, gaussianity, PCA, mahalanobis distance, priors and inductive biases, gpus technicals, gpu parallelism, scaling economics, data-centric ml, enviornment-centric rl, intrinsic motivation, world modelling, recurrent computation, memory architectures, fine tuning, pretraining, interpretability, probes and steering, dictionary learning, RAG, LLMs, VLMs, omni-models, tokens, vector quantizer, video models, individuality, priors, transformer economics, kv cache, positional encoding, mixture of experts, lora, self supervised learning, simclr, clip, rlhf rlaif, distillation, context distillation, on policy distillation, ppo, dpo, adam, adamW weight decay, sgd, batch size, dropout, regularization, double descent, grokking, lottery ticket hypothesis, sharpness, flow matching, normalizing flows, energy based models, CKA, tsne, umap, EM algorithm, RNNs, LSTMs, GRU, multi-agent RL, self-play, Nash equilibria, offline RL, model-based RL, long horizon problem, credit assignment problem, sparse reward problem, evolutionary methods, inference time compute and reasoning, active learning, calibration
